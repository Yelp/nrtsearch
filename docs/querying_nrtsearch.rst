Building Nrtsearch Queries
==========================

Nrtsearch mostly exposes the query classes in Lucene along with some other types of queries. These queries can be combined to get the desired ranking logic.

.. toctree::
   :maxdepth: 1
   :glob:

   queries/*
